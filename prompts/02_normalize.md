# Промпт: нормализация

Для каждой строки создай `source_row_id` и стабильный `operation_id`. Преобразуй `Кредит` в `money_in`, `Дебет` в `money_out`, код 643 — в RUB. Сохрани исходные поля без изменений. Отметь ошибки даты, валюты, суммы и направления.
